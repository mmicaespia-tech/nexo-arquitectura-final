{
  "name": "nexo-arquitectura-final",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "sass scss/styles.scss css/styles.css --no-source-map",
    "watch": "sass --watch scss/styles.scss css/styles.css --no-source-map"
  },
  "devDependencies": {
    "sass": "^1.93.2"
  }
}
